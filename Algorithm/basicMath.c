#include "basicMath.h"

float f_abs(float num)
{
    if(num >= 0)
        return num;
    else
        return -num;
}


s16 s16_abs(s16 num)
{
    if(num >= 0)
        return num;
    else
        return -num;
}
